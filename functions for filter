  let arr = [1, 2, 3, 4, 5, 6, 7];
  
  function inBetween(a, b) {
    return function bet(item) {
      if (item >= a && item <= b) return true;
      return false;
    }
  }
  
  function inArray(arr) {
      return function ar ( item ) {
        return arr.includes(item);
      }
  }
