function camelize (value){
  let arr = value.split ("-");
  for( let i = 1; i < arr.length; i++) {
    let str = arr[i];
    str = str[0].toUpperCase() + str.slice (1);
    arr[i] = str;
  }
   return arr.join("");
}
